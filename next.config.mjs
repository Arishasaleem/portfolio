/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['avatars.githubusercontent.com'], // Allow GitHub profile images
    }
};
// next.config.js
 
  
export default nextConfig;
